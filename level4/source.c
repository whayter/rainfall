#include <stdlib.h>
#include <stdio.h>

int m;

void p(char* param)
{
    printf(param_1);
    return ;
}

void n()
{
    char buf[520];
    
    fgets(buf, 512, stdin);
    p(buf);
    if (m == 16930116)
        system("/bin/cat /home/user/level5/.pass");
    return ;
}

int main(void)
{
    n();
    return (0);
}